export function App() {
  return (
    <main className="mx-auto flex min-h-dvh max-w-3xl flex-col justify-center gap-4 px-6 py-12">
      <p className="text-sm font-medium uppercase tracking-[0.2em] text-zinc-500">
        TMDB Movies
      </p>
      <h1 className="text-3xl font-semibold tracking-tight text-zinc-900 sm:text-4xl">
        Base pronta
      </h1>
      <p className="max-w-prose text-base leading-relaxed text-zinc-600">
        Estrutura feature-based, Tailwind e config de ambiente configurados.
        Próximo passo: roteamento com as páginas obrigatórias.
      </p>
    </main>
  )
}
